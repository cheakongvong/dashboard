name: dashboard
description: Dashboard Tomorrow
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Firebase Authentication
  firebase_core: ^2.15.1
  firebase_auth: ^4.9.0

  # Firebase Cloud Firestore
  cloud_firestore: ^4.9.1

  # Firebase Storage
  firebase_storage: ^11.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/